function handleBackButtonClick() {
    // Ambil tombol dengan ID "dropdown-item"
    const backendButton = document.getElementById('dropdown-item').querySelector('DropdownItem').click();
    if (backendButton) {
        // Trigger klik pada tombol "Backend"
        backendButton
    } else {
        console.error('Dropdown item not found');
    }
}